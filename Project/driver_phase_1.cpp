#include <iostream>
#include <vector>
#include <algorithm>  // For std::find
#include "Food.h"
#include "Customer.h"


int main() 
{   
    std::cout << std::endl;

     // Instantiate food container
    std::vector<Food> foodContainer;

    // Instantiate customer container
    std::vector<Customer> customerContainer;

    // Instantiate food container iterator
    std::vector<Food>::iterator foodIterator;

    // Instantiate customer container iterator
    std::vector<Customer>::iterator customerIterator;

    // instantiate food container
    // instantiate customer container
    // instantiate food container iterator
    // instantiate customer container iterator
    // read into A container
    // read into B container

    // test all functions for types A and B

    // delete all dynamic memory

    std::cout << std::endl;
    return 0;
}