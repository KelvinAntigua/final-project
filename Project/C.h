#ifndef EMPLOYEE_H
#define EMPLOYEE_H

class C {
    public:
        static void addB(/*B container */,  /* all parameters requied to construct a B object */);
        static bool dropB(/*B container */, /* B object */);
        static /* B iterator */ findB(/* B container */, /* data to look for (see document) */);   
};

#endif